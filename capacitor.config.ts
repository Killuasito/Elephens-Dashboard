import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.elephens.dashboard',
  appName: 'Elephens Dashboard',
  webDir: 'out'
};

export default config;
